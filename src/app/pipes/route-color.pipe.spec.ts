import { RouteColorPipe } from './route-color.pipe';

describe('RouteColorPipe', () => {
  it('create an instance', () => {
    const pipe = new RouteColorPipe();
    expect(pipe).toBeTruthy();
  });
});
