import { RouteDirectionPipe } from './route-direction.pipe';

describe('RouteDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new RouteDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
